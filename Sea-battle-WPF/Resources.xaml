<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Палитра -->
    <Color x:Key="PrimaryBlue">#E6F3FF</Color>
    <Color x:Key="SecondaryBlue">#B3D9FF</Color>
    <Color x:Key="AccentPurple">#D6CCFF</Color>
    <Color x:Key="DarkPurple">#8A7FBC</Color>
    <Color x:Key="TextColor">#2C3E50</Color>
    <Color x:Key="WaterColor">#F0F8FF</Color>
    <Color x:Key="ShipColor">#5D6D7E</Color>
    <Color x:Key="HitColor">#FF6B6B</Color>
    <Color x:Key="MissColor">#A0D1E8</Color>

    <!-- Кисти -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryBlue}"/>
    <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryBlue}"/>
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentPurple}"/>
    <SolidColorBrush x:Key="DarkBrush" Color="{StaticResource DarkPurple}"/>
    <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>
    <SolidColorBrush x:Key="WaterBrush" Color="{StaticResource WaterColor}"/>
    <SolidColorBrush x:Key="ShipBrush" Color="{StaticResource ShipColor}"/>
    <SolidColorBrush x:Key="HitBrush" Color="{StaticResource HitColor}"/>
    <SolidColorBrush x:Key="MissBrush" Color="{StaticResource MissColor}"/>

    <!-- Стили кнопок -->
    <Style x:Key="ModernButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource DarkBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="15,8"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource DarkBrush}"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Стиль текстовых блоков -->
    <Style x:Key="HeaderText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,10"/>
    </Style>

    <!-- Стиль для ячеек поля -->
    <Style x:Key="CellStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource WaterBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SecondaryBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Width" Value="25"/>
        <Setter Property="Height" Value="25"/>
        <Setter Property="Margin" Value="1"/>
    </Style>

</ResourceDictionary>